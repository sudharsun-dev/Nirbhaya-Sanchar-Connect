export default function CallControls({ muted, onMute, onEnd }) {
  return <div className="call-controls"><button className={muted ? 'control-button muted' : 'control-button'} onClick={onMute} aria-label={muted ? 'Unmute microphone' : 'Mute microphone'}><span aria-hidden="true">{muted ? '◉' : '◌'}</span><small>{muted ? 'Unmute' : 'Mute'}</small></button><button className="control-button" aria-label="Audio output"><span aria-hidden="true">◒</span><small>Audio</small></button><button className="end-button" onClick={onEnd} aria-label="End call"><span aria-hidden="true">☎</span><small>End call</small></button></div>
}