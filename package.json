{
  "name": "nirbhaya-sanchar-workspace",
  "private": true,
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "@vercel/postgres": "^0.10.0",
    "bcryptjs": "^3.0.3",
    "livekit-server-sdk": "^2.15.0"
  },
  "scripts": {
    "start": "npm run server",
    "dev": "npm --prefix connect run dev --",
    "server": "npm --prefix connect/server run start",
    "build": "npm --prefix connect run build",
    "lint": "npm --prefix connect run lint",
    "preview": "npm --prefix connect run preview"
  }
}
